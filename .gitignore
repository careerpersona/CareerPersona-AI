# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Secrets — never commit these (CLOUDFLARE_API_TOKEN, etc.)
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.wrangler/
worker-backup.js

# Developer toolkit generated output (docs/architecture/Developer-Toolkit-Architecture.md) --
# report artifacts are reproducible from the tools themselves, not source.
tools/reports/

# verify-referral-intelligence-responsive.cjs's screenshot output -- regenerated
# every run (including via tools/regression-runner), throwaway by design.
scripts/verify/_rc_screens/

# Manual QA screenshots -- point-in-time comparison shots, not living docs.
QA/

# Auto-generated by `wrangler types` (env binding types for worker.js editor
# hints). Regenerable, hash-stamped to a specific workerd build -- not source.
worker-configuration.d.ts
